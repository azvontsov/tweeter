// require dependecies
const express = require('express');
// initialize the express app
const app = express();

// configure settings
const PORT = 3000;

// connect to and config mongodb

// mounting middleware

// mount routes



// tell the app to listen
app.listen(PORT, () => {
    console.log(`listening port on 3000:${PORT}`);
})